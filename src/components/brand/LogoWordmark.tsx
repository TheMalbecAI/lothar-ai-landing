export function LogoWordmark({ className = "" }: { className?: string }) {
  return (
    <span
      className={`font-heading text-xl font-bold tracking-[0.2em] uppercase text-cream ${className}`}
    >
      Lothar AI
    </span>
  );
}
